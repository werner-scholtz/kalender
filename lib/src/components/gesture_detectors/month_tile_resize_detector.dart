import 'package:flutter/material.dart';

/// TODO: implement this.
class MonthTileResizeGestureDetector extends StatelessWidget {
  const MonthTileResizeGestureDetector({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
