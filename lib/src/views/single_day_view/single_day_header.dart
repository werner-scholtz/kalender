import 'package:flutter/material.dart';

class SingleDayHeader<T extends Object?> extends StatelessWidget {
  const SingleDayHeader({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
