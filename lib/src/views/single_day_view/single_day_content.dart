import 'package:flutter/material.dart';

class SingleDayContent<T extends Object?> extends StatelessWidget {
  const SingleDayContent({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
