import 'package:flutter/material.dart';

/// TODO: Add schedule view.
class ScheduleView<T extends Object?> extends StatefulWidget {
  const ScheduleView({super.key});

  @override
  State<ScheduleView<T>> createState() => _ScheduleViewState<T>();
}

class _ScheduleViewState<T extends Object?> extends State<ScheduleView<T>> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
